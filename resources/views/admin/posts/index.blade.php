@extends('layouts.admin')
@section('content')
<section class="mx-auto max-w-7xl"><div class="flex items-center justify-between"><div><p class="font-mono text-sm text-orange-600">// content management</p><h1 class="mt-2 text-3xl font-bold">Artikel</h1></div><a href="{{ route('admin.posts.create') }}" class="rounded-lg bg-orange-600 px-4 py-2.5 text-sm font-semibold text-white">+ Artikel baru</a></div>@if(session('success'))<p class="mt-6 rounded-lg bg-green-100 p-3 text-sm text-green-800">{{ session('success') }}</p>@endif<div class="mt-8 overflow-x-auto rounded-xl border border-stone-200 bg-white dark:border-stone-800 dark:bg-stone-900"><table class="w-full min-w-[640px] text-left text-sm"><thead class="bg-stone-100 dark:bg-stone-800"><tr><th class="p-4">Judul</th><th class="p-4">Status</th><th class="p-4">Tanggal</th><th class="p-4"></th></tr></thead><tbody>@forelse($posts as $post)<tr class="border-t border-stone-200 dark:border-stone-800"><td class="p-4 font-medium">{{ $post->title }}<div class="mt-1 text-xs text-stone-500">{{ $post->category?->name ?? 'Tanpa kategori' }}</div></td><td class="p-4">{{ $post->status }}</td><td class="p-4 text-stone-500">{{ $post->published_at?->format('d M Y') ?? '—' }}</td><td class="p-4 text-right"><a href="{{ route('admin.posts.edit',$post) }}" class="text-orange-600">Edit</a></td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-stone-500">Belum ada artikel.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $posts->links() }}</div></section>
@endsection
